services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: metaplogging
      POSTGRES_USER: metaplogging
      POSTGRES_PASSWORD: metaplogging_dev
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
